import React from 'react';
import { Button } from 'antd';

export default class List extends React.Component {
  render() {
    return (
    	<div>
        	<Button type="primary">角色</Button>
        </div>
    );
  }
}

